doctype html
html
  head
    link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css' rel='stylesheet' integrity='sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH' crossorigin='anonymous')
    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js' integrity='sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz' crossorigin='anonymous')
    script(src='https://kit.fontawesome.com/934566fbc5.js', crossorigin='anonymous')
    link(rel='stylesheet', href='/css/styles.css')  
    title Payments
  body
    .container-fluid
      div.d-flex.flex-row.justify-content-between
        a.btn.btn-primary.m-0(href="/admin/dashboard") <i class="fa-solid fa-house"></i> Dashboard 
        h2.text-center.m-0 Payments
        a.btn.btn-primary.m-0(href="/payments/new") <i class="fa-solid fa-circle-plus"></i> Add New Payment
      .container-fluid.dashboard
        .container-fluid.d-flex.gap-5.flex-wrap.ultimaHora
              each payment in payments
                a.indexCard.text-dark(href=`/payments/${payment._id}`)
                  div.d-flex.flex-column.h-100
                    div.indexTitle
                      h5 <i class="fa-solid fa-hospital"></i>
                      h5.card-title="Id: " + payment._id
                    div.groove
                    div.d-flex.flex-column
                      p.d-flex.flex-column 
                        strong Amount:
                        | $#{payment.amount}
                      p.d-flex.flex-column 
                        strong Date:
                        | #{payment.date}
                      p.d-flex.flex-column 
                        strong Status:
                        | #{payment.status}                   
                    div.actionCard Details <i class="fa-solid fa-chevron-right"></i>

  
  
  
  
        //- table.table.table-bordered
        //-   thead.table-light
        //-     tr
        //-       th ID
        //-       th Amount
        //-       th Date
        //-       th Status
        //-       th Actions
        //-   tbody
        //-     each payment in payments
        //-       tr
        //-         td= payment._id
        //-         td= payment.amount
        //-         td= payment.date
        //-         td= payment.status
        //-         td.d-flex.flex-column
        //-           a.btn.btn-primary.m-1(href=`/payments/${payment._id}`) View
        //-           a.btn.btn-primary.m-1(href=`/payments/${payment._id}/edit`) Edit
        //-           form(action=`/payments/${payment._id}?_method=DELETE` method="POST" style="display:inline")
        //-             button.btn.btn-danger.m-1(type="submit") Delete
